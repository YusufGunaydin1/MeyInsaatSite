# Dependencies
node_modules/

# Build output
dist/
.astro/
.output/
.vercel/
.netlify/

# Environment
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Windows Alternate Data Stream sidecars (mark-of-the-web) — never publish
*Zone.Identifier*
*:Zone.Identifier

# Image source library (all generated candidates, used + unused) — maintained
# locally as a working library, NOT published. The site consumes only the images
# it needs, brought into the Astro project and optimized (WebP/AVIF) at build time.
images/

# Internal working docs — kept local, not published to this public repo:
# AI-development guides inherited from the ERP, plus the site design brief.
# Remove a line if you want that file published.
CLAUDE.md
AGENTS.md
MEY_INSAAT_DESIGN_BRIEF.md
# Pazarlama/reklam oyun kitabı — rakiplere açık bir depoda durmamalı.
MEY_GROWTH_RUNBOOK.md
MEY_GROWTH_APPENDIX/

# Playwright
test-results/
playwright-report/
blob-report/
e2e/.env

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
